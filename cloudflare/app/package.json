{
  "name": "produkter",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy --strict",
    "verify:production": "node ../scripts/verify-production.mjs app",
    "secret:set-provider-key": "wrangler secret put PROVIDER_CONFIG_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260906.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.0"
  }
}
